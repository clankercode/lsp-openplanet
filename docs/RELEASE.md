# Releasing openplanet-lsp

The canonical release procedure lives at the repository root:

**→ [../RELEASE.md](../RELEASE.md)**

That document covers version bumps, tagging, CI (`release.yml`), **writing
changelogs**, **updating GitHub Releases after CI**, npm publish, and the rule
to keep the procedure in sync when tooling changes.

Do not duplicate procedure here — edit `RELEASE.md` instead.
